import React from 'react'

const NewButton = ({name}) => {
  return (
    
      <button className='  m-2 p-2 bg-gray-500 rounded-lg  text-white text-sm font-semibold hover:bg-blue-950'>{name}</button>
    
  )
}

export default NewButton
